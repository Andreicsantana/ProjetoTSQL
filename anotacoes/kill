SELECT session_id, login_name, host_name, status
FROM sys.dm_exec_sessions
WHERE database_id = DB_ID('TsqlProject');

KILL session_id;

USE master;
GO
ALTER DATABASE TsqlProject SET SINGLE_USER WITH ROLLBACK IMMEDIATE;
GO
DROP DATABASE TsqlProject;
GO